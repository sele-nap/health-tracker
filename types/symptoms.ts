export type SymptomDefinitionProp = {
  id: string;
  name: string;
  unit: string | null;
  conditionName: string;
};

export type SymptomDefinition = {
  id: string;
  name: string;
  unit: string | null;
};
